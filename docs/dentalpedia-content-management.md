# Dentalpedia content directory

The 치카피디아 tab now opens `DentalpediaContentTab` following Figma `7960:68825`. It lists all three content types with title/category search, publication-state filters, title-only rows, exposure badges, Korea dates and server pagination. Titles and 수정하기 open the existing type-specific editor by its exact ID. 신규 콘텐츠 생성 opens a blank editor. 보관하기 and 삭제하기 require a confirmation, preserve the dialog on failure, and refresh the list on success.

The 2026-09-15 revision of node `7960:68825` in file `I63Qe9eqRYhlzRcSxglSQl` uses 68px rows without thumbnails, with vertically centered titles, status/exposure badges and action controls. Keep the 56px header, full-title tooltip and accessible edit label, title ellipsis, and narrow-screen table scrolling. List rows do not load thumbnail images; the API and editor media contracts stay unchanged.

`AdminNavigationProvider` owns the editor selection so the Dentalpedia document/shell scroll lock applies only while editing. The directory uses normal document scrolling and confines narrow-screen table overflow to the table region. `AdminSelect` remains the category picker. The row menu uses a viewport-positioned portal, keyboard navigation and Escape dismissal; native modal dialogs trap focus and initially focus Cancel.

Archive (`7960:69980`) and delete (`7960:70151`) confirmations share the existing `ContentActionDialog` and protected mutation flow. Both use a 400px white card, 16px radius, Figma shadow, 32/24px body padding and a 56×28px tinted pill containing the exported 28px info icon. Archive uses the blue icon, restoration guidance and teal action; delete uses the red icon, irreversible-deletion/exposure-removal guidance and red action. The light footer separates Cancel on the left from the action on the right. The target title remains available to assistive technology via `aria-describedby` without adding a visible row absent from Figma. Keep Escape/Cancel and focus trapping, disable dismissal and duplicate submission while busy, and show API errors inside the dialog for retry. Narrow/short viewports bound the card and allow vertical scrolling. Backdrop clicks do not confirm an action.

Successful empty results follow Figma `7960:69679` and replace both the table (including column headers) and pagination with a 500px white, bordered, 12px-radius panel. Its exported `content-empty.svg` icon is 40px inside an 80px light circle, with a 24px gap before the centered title and description. The unfiltered copy is `아직 등록된 콘텐츠가 없습니다.` / `치카피디아에 노출할 콘텐츠를 만들어보세요.`; an active type, status, category or search filter instead shows `검색 조건에 맞는 콘텐츠가 없습니다.` / `검색어나 필터를 변경해 보세요.`. Keep the header, create action and filters available. Narrow screens reduce horizontal padding and wrap copy without overflowing. Loading and request failures must never show this empty-success state.

The editors accept `initialContentId`, `onBack` and `onSaved`. An explicit ID may load draft, published or archived content; a failed load blocks all saves to prevent accidental duplicate creation. Content type changes are disabled while editing an existing ID. Publication updates preserve the stored publication timestamp; saving returns to the filtered list. Returning without saving asks only when editable values differ from the loaded baseline; reverting edits or changing previews does not prompt. Browser Back/Forward and the list button restore the directory/editor selection. See `admin-navigation.md`. The search icon uses canonical square geometry in a fixed 24×24px box. New editors always start blank; existing content loads only through its explicit directory ID.

Deploy API migration `20260914100000_dentalpedia_content_management.sql`, then the API, then Admin. The directory uses protected `/api/v1/admin/dentalpedia/content` requests; existing editors retain their type-specific API endpoints. See `../../ChikaPick_API/docs/dentalpedia-content-management.md` for state semantics, deletion/storage behavior and public-cache propagation. This change does not publish new content or apply the migration automatically.

Run `npm run test`, `npm run lint` and `npm run build`. Browser checks should use local fixture responses for all three editors, search/category/status filters, pagination, archive/delete confirmation and errors, missing-editor records, and a narrow viewport. Never use production content as a deletion fixture. Exported Figma directory icons are tracked under `public/dentalpedia/content-*.svg`.

For empty-state verification, use local empty responses at desktop and 390px widths, check filtered and unfiltered copy, confirm table headers/pagination are absent, and exercise loading, failure/retry, populated results and the create action. Capture screenshots from the local fixture without deleting production content or committing fixture routes.

For confirmation verification, capture each modal from local fixture rows, check 400px desktop and narrow-screen sizing, Cancel/Escape/focus behavior, and simulated pending/failure/success responses. Verify the selected type/ID and archive PATCH or delete DELETE request in the fixture; do not delete production content to obtain screenshots or test the UI.

## Publication-only editors (2026-09-18 PM feedback)

영상, 게시물, and 칼럼 no longer expose 임시저장 or submit draft writes. The footer has 취소 and 발행하기 (수정 완료 for published content). Both immediate and scheduled publication send `status: published` through the existing type-specific API, with all publication validation still required. Browser local-storage record IDs are no longer read or written for automatic draft resume. Existing draft rows remain in the directory for explicit editing/publication or deletion; this UI change does not purge stored content or require an API/schema migration.

Verify all three editors with local fixture data: no draft-save action; new content remains blank even with legacy local-storage IDs; incomplete publication is blocked; successful create/update sends published status; scheduled dates and original published timestamps survive; API failures retain entered values; failed explicit loads block saving; and Cancel still honors unsaved-change confirmation. Check the two-action footer at desktop and narrow widths. Do not publish production content during verification.
